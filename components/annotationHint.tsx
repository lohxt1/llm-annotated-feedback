const AnnotationHint = () => {
  return (
    <div className="ml-4 flex flex-row text-xs">
      <label className="mr-1 text-slate-300 dark:text-slate-500">
        Select the text to start labeling
      </label>
      <label className="underline"></label>
    </div>
  );
};

export default AnnotationHint;
